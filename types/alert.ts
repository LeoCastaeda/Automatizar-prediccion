/**
 * AlertType enum
 * Defines the types of alerts supported by the system
 */
export enum AlertType {
  PRICE = 'PRICE',
  CHANGE_PERCENT = 'CHANGE_PERCENT'  // Renamed from CHANGE_% for valid TypeScript identifier
}
